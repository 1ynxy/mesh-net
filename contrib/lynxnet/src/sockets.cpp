#include "sockets.h"

// Socket //

// Constructors & Destructors

Socket::Socket() {

}

Socket::Socket(int sock, SockType type) : sock(sock), type(type) {
	port = inf.
}

Socket::~Socket() {

}

// Member Functions


